package leetcode

func leetcode2341(nums []int) []int {
	ansswer := make([]int, 2)

	for i := 0; i < len(nums); i++ {

	}

	ansswer[0] = 1
	ansswer[1] = 2

	return ansswer
}

// 先排序, 快排
func fastSort(a []int) []int {
	// 选一个分界值,通常选第一个
	dem := a[0]
	// 左边
	left := 0
	// 右边
	right := len(a) - 1
	for left < right {
		for {

		}
	}
}
